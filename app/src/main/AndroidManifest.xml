<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantOrderApp"
        tools:targetApi="31">

        <!-- Welcome Activity as launcher -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:launchMode="singleTop">

            <!-- Uncomment this to make this page a launcher page
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            -->
        </activity>

        <!-- Admin related activities -->
        <activity
            android:name=".AdminLoginActivity"
            android:exported="false"
            android:noHistory="true"/> <!-- This prevents going back to login -->

        <!-- Change WelcomeActivity to AdminActivity in the launcher intent-filter -->
        <activity
            android:name=".AdminActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <!-- remove below to make welcomeActivity the launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Rest of your activities -->
        <activity
            android:name=".AddLunchItemActivity"
            android:label="Lägg till lunch"
            android:parentActivityName=".LunchManagementActivity" />
        <activity android:name=".EditLunchItemActivity" android:exported="false"/>
        <activity android:name=".WeeklyMenuPlanningActivity" android:exported="false"/>

        <activity android:name=".ManageMenuActivity" android:exported="false"/>
        <activity android:name=".LunchManagementActivity" android:exported="false"/>
        <activity android:name=".MainActivity" android:exported="false"/>
        <activity android:name=".OrderActivity" android:exported="false"/>
        <activity android:name=".KitchenActivity" android:exported="false"/>
        <activity android:name=".ManageOrdersActivity" android:exported="false"/>
        <activity android:name=".BookingActivity" android:exported="false"/>

    </application>
</manifest>