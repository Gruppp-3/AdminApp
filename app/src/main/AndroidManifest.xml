<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantOrderApp"
        tools:targetApi="31" >

        <activity android:name=".OrderActivity" />
        <activity android:name=".ManageMenuActivity" android:exported="true"/>
        <activity android:name=".ViewStatsActivity" android:exported="true"/>
        <activity android:name=".ManageOrdersActivity" android:exported="true"/>
        <activity android:name=".BookingActivity" android:exported="true"/>



        <activity
            android:name=".AdminLoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lägg till AdminActivity här -->
        <activity android:name=".AdminActivity" android:exported="true"/>

        <activity android:name=".KitchenActivity" android:exported="false"/>
    </application>

</manifest>
